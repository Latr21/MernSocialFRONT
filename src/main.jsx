
import React, { StrictMode } from "react";
import ReactDOM from "react-dom/client";
import Timeline from "./components/Timeline";

ReactDOM.createRoot(document.getElementById("root")).render(
    <StrictMode><Timeline /></StrictMode>
);
